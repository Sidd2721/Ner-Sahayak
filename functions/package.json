{
  "name": "ner-sahayak-functions",
  "version": "0.1.0",
  "private": true,
  "description": "Firebase Cloud Functions for NER Sahayak — milestone-2-backend.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "serve": "firebase emulators:start --only functions,firestore,auth",
    "test": "node --import tsx/esm --test --test-concurrency=1 test/**/*.test.ts",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.5.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
