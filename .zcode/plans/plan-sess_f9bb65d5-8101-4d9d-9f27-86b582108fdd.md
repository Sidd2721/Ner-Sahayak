Stage 2 — milestone-3-mobile (apps/mobile/ ONLY), approved by user message: "Plan verified and approved... Proceed with Phases 0-12."

Steps:
1. Verify PR #1 merged (gh pr view 1); fetch/pull latest main (user added docs/NER-Sahayak-Offline-App-System-Design-and-Build-Guide.md to main); branch milestone-3-mobile.
2. Phase 0 gate: packages/shared exports + en/hi/as i18n present (already true, 46/46 tests).
3. Phase 1: apps/mobile scaffold — package.json (type module, dev/build/preview), vite.config.js (port 3001, dist, @shared alias), deps dexie+firebase, vite devDep; index.html created FRESH from scratch (user confirmed no MVP exists — do not block); manifest.json; checkpoint: npm run dev clean on :3001.
4. Phase 2: src/db.js — Dexie tables exactly per guide (reports, outbox, corridorState, districts, session).
5. Phase 3: sw.js — cache-first shell, skipWaiting()+clients.claim(), skip firestore.googleapis.com/firebaseio.com/googleapis.com, sync→FLUSH_OUTBOX; page controllerchange reload.
6. Phase 4: src/sync.js — SyncEngine: online/visibilitychange/foreground-interval flush (iOS-safe), Background Sync bonus only, FIFO, 8-retry poison cap, idempotent client-UUID creates.
7. Phases 5-9: report-form.js (GPS-denied fallback), status-board.js (Dexie-only render, onSnapshot merge, seed from @shared/constants/corridors), risk.js (thin wrapper on shared calcRisk), i18n.js (static en/hi/as imports, localStorage, full re-render), auth.js (session cache, Reporter/Authority role), main.js boot.
8. Firebase: apps/mobile/.env + .env.example — emulator-only values (VITE_USE_EMULATOR=true, hosts 127.0.0.1:9099/:8080, projectId mirrored from repo config). No real keys.
9. Adapt calls to the REAL shared API (calcPriorityKey object arg, calcRisk result object, real @shared paths) — flagged in PR.
10. Verify on desktop localhost: offline submit → outbox=1; online+foreground → drains to 0; offline relaunch → board renders; 3-language toggle; GPS-denied fallback; kill-mid-sync no duplicates.
11. Phase 12 boundary: stop at clean `npm run build` → apps/mobile/dist/. NO firebase deploy, no real credentials.
12. PR milestone-3-mobile → main with the 10-row completion table (device/iOS/public-URL rows = "pending human deploy verification"). Do NOT merge. Report URL and stop.